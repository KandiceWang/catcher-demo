import React, { useState } from 'react';import { Navbar } from '../navigation/Navbar';interface HeroSectionProps {  onSearch: (query: string) => void;}export const HeroSection: React.FC<HeroSectionProps> = ({ onSearch }) => {  const [activeTab, setActiveTab] = useState<'talent' | 'jobs'>('talent');  const [searchQuery, setSearchQuery] = useState('');  const handleSearch = () => {    onSearch(searchQuery);  };  const handleKeyPress = (e: React.KeyboardEvent) => {    if (e.key === 'Enter') {      handleSearch();    }  };  return (    <div className="relative min-h-screen overflow-hidden">      {/* Background with Taiwan-inspired gradient */}      <div className="absolute inset-0 bg-gradient-to-br from-catcher-coral via-catcher-pink to-catcher-sky opacity-90"></div>            {/* Background pattern overlay */}      <div className="absolute inset-0 opacity-10">        <svg className="w-full h-full" viewBox="0 0 100 100" preserveAspectRatio="none">          <defs>            <pattern id="hero-pattern" x="0" y="0" width="20" height="20" patternUnits="userSpaceOnUse">              <circle cx="10" cy="10" r="1" fill="white" opacity="0.3"/>            </pattern>          </defs>          <rect width="100" height="100" fill="url(#hero-pattern)"/>        </svg>      </div>      {/* Navigation */}      <Navbar isTransparent={true} />      {/* Hero Content */}      <div className="relative z-10 flex items-center justify-center min-h-screen px-6 pt-20">        <div className="text-center text-white max-w-5xl">          {/* Main Heading */}          <h1 className="text-5xl md:text-7xl font-black mb-6 tracking-tight">            連接台灣            <span className="block text-catcher-yellow">頂尖人才</span>          </h1>                    {/* Subtitle */}          <p className="text-xl md:text-2xl mb-12 text-white/90 font-light max-w-3xl mx-auto leading-relaxed">            在地化的專業平台，為台灣企業與自由工作者打造最佳媒合體驗          </p>          {/* Tab Switcher */}          <div className="flex justify-center mb-8">            <div className="bg-white/20 backdrop-blur-md rounded-2xl p-2 flex border border-white/30">              <button                onClick={() => setActiveTab('talent')}                className={`px-8 py-4 rounded-xl font-semibold transition-all duration-300 ${                  activeTab === 'talent'                    ? 'bg-white text-catcher-coral shadow-lg transform scale-105'                    : 'text-white hover:bg-white/20'                }`}              >                尋找人才              </button>              <button                onClick={() => setActiveTab('jobs')}                className={`px-8 py-4 rounded-xl font-semibold transition-all duration-300 ${                  activeTab === 'jobs'                    ? 'bg-white text-catcher-coral shadow-lg transform scale-105'                    : 'text-white hover:bg-white/20'                }`}              >                瀏覽工作              </button>            </div>          </div>          {/* Search Interface */}          <div className="bg-white/20 backdrop-blur-md rounded-2xl p-8 max-w-3xl mx-auto border border-white/30">            <div className="flex flex-col md:flex-row gap-4">              <div className="flex-1">                <input                  type="text"                  value={searchQuery}                  onChange={(e) => setSearchQuery(e.target.value)}                  onKeyPress={handleKeyPress}                  placeholder={                    activeTab === 'talent'                       ? '搜尋技能、職位或專業領域...'                       : '搜尋工作機會或專案類型...'                  }                  className="w-full px-6 py-4 rounded-xl border-0 bg-white/90 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-catcher-yellow focus:bg-white transition-all text-lg"                />              </div>              <button                 onClick={handleSearch}                className="bg-catcher-yellow text-gray-900 px-10 py-4 rounded-xl hover:bg-catcher-mint transition-all font-bold text-lg shadow-lg hover:shadow-xl transform hover:scale-105"              >                搜尋              </button>            </div>                        {/* Popular Tags */}            {activeTab === 'talent' && (              <div className="mt-6 flex flex-wrap gap-3 justify-center">                {[                  '網頁開發', 'UI/UX設計', '數據分析',                   '內容寫作', '數位行銷', 'APP開發',                  '平面設計', '影片剪輯'                ].map((skill) => (                  <button                    key={skill}                    onClick={() => setSearchQuery(skill)}                    className="bg-white/20 text-white px-4 py-2 rounded-full text-sm hover:bg-white/30 cursor-pointer transition-all border border-white/30 hover:border-white/50"                  >                    {skill}                  </button>                ))}              </div>            )}            {activeTab === 'jobs' && (              <div className="mt-6 flex flex-wrap gap-3 justify-center">                {[                  '遠端工作', '兼職專案', '長期合作',                   '急件處理', '技術顧問', '創意設計'                ].map((jobType) => (                  <button                    key={jobType}                    onClick={() => setSearchQuery(jobType)}                    className="bg-white/20 text-white px-4 py-2 rounded-full text-sm hover:bg-white/30 cursor-pointer transition-all border border-white/30 hover:border-white/50"                  >                    {jobType}                  </button>                ))}              </div>            )}          </div>          {/* Trust Indicators */}          <div className="mt-12 flex flex-col md:flex-row items-center justify-center gap-8 text-white/80">            <div className="flex items-center gap-2">              <div className="w-2 h-2 bg-catcher-yellow rounded-full"></div>              <span className="text-sm font-medium">超過 10,000+ 專業人才</span>            </div>            <div className="flex items-center gap-2">              <div className="w-2 h-2 bg-catcher-mint rounded-full"></div>              <span className="text-sm font-medium">500+ 企業信賴選擇</span>            </div>            <div className="flex items-center gap-2">              <div className="w-2 h-2 bg-catcher-sky rounded-full"></div>              <span className="text-sm font-medium">平均 48 小時內媒合成功</span>            </div>          </div>        </div>      </div>    </div>  );};